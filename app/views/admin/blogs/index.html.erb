<section class="blogs">
  <div class="container">
   <table class="table">
     <thead>
       <tr>
          <th class="col-4">First</th>
          <th class="col">Last</th>
          <th class="col">Handle</th>
      </tr>
     </thead>
   <tbody>
     <% @blog.each do |blog| %>
    <tr>
      <td><%= blog.title %></td>
      <td><%= blog.description %></td>
      <td><%= link_to "Mostrar",admin_blog_path(blog), class:"btn btn-outline-primary"%></td>
      <td><%= link_to "Editar", edit_admin_blog_path(blog), class:"btn btn-outline-success"%></td>
       <td><%= link_to "Eliminar",admin_blog_path(blog), method: :delete, data: { confirm: "¿Estás seguro de eliminar este dato"}, class:"btn btn-outline-danger"%>
       </td>
     </tr>

    <% end %>
  </tbody>
 </table>
   <%= link_to "New", new_admin_blog_path, class:"btn btn-outline-primary"%>
 </div>
</section>