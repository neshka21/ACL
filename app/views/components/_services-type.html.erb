<section class="services">
	<div class="row justify-center">
		<div class="col-3">
			<% @service.each do |service|%>
			<%= image_tag service.imgs.thumb.url, width:"90px"%>
			<h2><%= service.title%></h2>
			<p><%= service.description%></p>
			<% end %>
		</div>
	</div>
</section>